## 均摊分析
- 多个操作，一起算时间复杂度。一次性的操作，平均到整体来看。
- MULTIPOP 队列
	- 一次性出队 k 个元素
	- 总量是固定的，每个元素只出入队列一次。
- vector
	- insert
		- 一旦元素超过容量限制，则扩大一倍，再复制。
		- 每次 copy 是 O(n)，但不是每一次都触发 copy 操作。
		- 证明
	- delete
		- 自动释放空间
		- insert 过程的逆过程

	